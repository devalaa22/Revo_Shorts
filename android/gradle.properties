# زيادة الذاكرة لتجنب انهيار الـ Gradle daemon
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# استخدام AndroidX
android.useAndroidX=true
android.enableJetifier=true

# إعدادات التوقيع
keyAlias=dramix
keyPassword=dramix
storeFile=dramix.jks
storePassword=dramix

# ضبط JDK الصحيح (ضروري لحل مشكلة crash)
#org.gradle.java.home=C:/JDK/jdk-17
